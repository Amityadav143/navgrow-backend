server.port=8081
spring.datasource.url=jdbc:postgresql://localhost:5432/navgrow_prod
spring.datasource.username=navgrow_user
spring.datasource.password=Navgrow@jul25
spring.jpa.hibernate.ddl-auto=validate

# Production URLs — OAuth success redirect + email links land on the live site.
app.frontend-url=${FRONTEND_URL:https://navgrow.org}
# Browsers allowed to call this API (comma-separated; overridable via env).
app.allowed-origins=${ALLOWED_ORIGINS:https://navgrow.org,https://www.navgrow.org}
